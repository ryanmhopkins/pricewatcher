const $=s=>document.querySelector(s), list=$('#list'), msg=$('#msg'), count=$('#count');
const EDGE='https://hmsldwpcaupanooestfr.supabase.co/functions/v1/pricewatcher-api';
let session=localStorage.getItem('pw_session')||'';
async function request(action,payload={},useSession=true){const headers={'Content-Type':'application/json'};if(useSession&&session)headers['x-pricewatcher-session']=session;const r=await fetch(EDGE,{method:'POST',headers,body:JSON.stringify({action,...payload})});const text=await r.text();let d;try{d=JSON.parse(text)}catch{throw new Error(text||`Request failed (${r.status})`)}if(!r.ok){const e=new Error(d.error||'Request failed');e.status=r.status;throw e}return d}
const api=(action,payload={})=>request(action,payload,true);
function addLogout(){if($('#logout'))return;const b=document.createElement('button');b.id='logout';b.className='logout';b.textContent='Lock';b.onclick=()=>{localStorage.removeItem('pw_session');session='';location.reload()};document.querySelector('header').appendChild(b)}
function showGate(message=''){document.body.classList.add('locked');let gate=$('#gate');if(!gate){gate=document.createElement('div');gate.id='gate';gate.innerHTML=`<div class="gate-card"><div class="gate-icon">↗</div><p>PRIVATE BETA</p><h1>PriceWatcher</h1><div class="gate-copy">Enter your access code to open the monitoring dashboard.</div><form id="gate-form"><input id="gate-pass" type="password" autocomplete="current-password" placeholder="Access code" required><button>Unlock PriceWatcher →</button></form><div id="gate-msg"></div></div>`;document.body.appendChild(gate);$('#gate-form').onsubmit=async e=>{e.preventDefault();const gm=$('#gate-msg');gm.textContent='Unlocking…';try{const d=await request('login',{passphrase:$('#gate-pass').value},false);session=d.token;localStorage.setItem('pw_session',session);gate.remove();document.body.classList.remove('locked');addLogout();await load()}catch(err){gm.textContent=err.message}}}$('#gate-msg').textContent=message}
async function load(){try{msg.textContent='';const rows=await api('list');addLogout();count.textContent=`${rows.length} competitor${rows.length===1?'':'s'}`;list.innerHTML=rows.length?'':'<div class="empty">No competitors yet. Add your first pricing page.</div>';rows.forEach(m=>{const changes=[...(m.changes||[])].sort((a,b)=>new Date(b.created_at)-new Date(a.created_at));const c=changes[0];const el=document.createElement('article');el.className='monitor';el.innerHTML=`<div class="top"><div><h3>${esc(m.name)}</h3><a href="${esc(m.url)}" target="_blank" rel="noopener">${esc(m.url)}</a></div><span class="status ${esc(m.last_status)}">${esc(m.last_status)}</span></div><div class="meta"><span>Last checked: ${m.last_checked_at?new Date(m.last_checked_at).toLocaleString():'Never'}${changes.length?` · ${changes.length} change${changes.length===1?'':'s'} detected`:''}</span><div class="actions"><button class="check">Check now</button><button class="delete">Remove</button></div></div>${c?`<div class="change"><b>Latest change · ${new Date(c.created_at).toLocaleString()}</b>${(c.added_lines||[]).slice(0,5).map(x=>`<div class="plus">+ ${esc(x)}</div>`).join('')}${(c.removed_lines||[]).slice(0,5).map(x=>`<div class="minus">− ${esc(x)}</div>`).join('')}</div>`:''}`;el.querySelector('.check').onclick=()=>check(m.id);el.querySelector('.delete').onclick=()=>removeMonitor(m.id,m.name);list.appendChild(el)})}catch(e){if(e.status===401){localStorage.removeItem('pw_session');session='';showGate('Session expired. Enter the access code again.')}else msg.textContent=`Connection error: ${e.message}`}}
async function check(id){msg.textContent='Checking…';try{const d=await api('check',{id});msg.textContent=d.changed?'Pricing change detected.':'No pricing change detected.';await load()}catch(e){msg.textContent=e.message}}
async function removeMonitor(id,name){if(!confirm(`Stop monitoring ${name}?`))return;msg.textContent='Removing monitor…';try{await api('delete',{id});msg.textContent='Monitor removed.';await load()}catch(e){msg.textContent=e.message}}
$('#form').onsubmit=async e=>{e.preventDefault();msg.textContent='Capturing first snapshot…';try{const d=await api('create',{name:$('#name').value.trim(),url:$('#url').value.trim()});$('#name').value='';$('#url').value='';msg.textContent=d.warning?`Added, initial check warning: ${d.warning}`:'Monitor added and first snapshot captured.';await load()}catch(e){msg.textContent=e.message}};$('#refresh').onclick=load;function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
if(session)load();else showGate();
