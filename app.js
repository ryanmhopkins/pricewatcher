const $=s=>document.querySelector(s), list=$('#list'), msg=$('#msg'), count=$('#count');
async function api(path,opts){const r=await fetch(path,opts);const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');return d}
async function load(){try{const rows=await api('/api/monitors');count.textContent=`${rows.length} competitor${rows.length===1?'':'s'}`;list.innerHTML=rows.length?'':'<div class="empty">No competitors yet. Add your first pricing page.</div>';rows.forEach(m=>{const changes=[...(m.changes||[])].sort((a,b)=>new Date(b.created_at)-new Date(a.created_at));const c=changes[0];const el=document.createElement('article');el.className='monitor';el.innerHTML=`<div class="top"><div><h3>${esc(m.name)}</h3><a href="${esc(m.url)}" target="_blank">${esc(m.url)}</a></div><span class="status ${esc(m.last_status)}">${esc(m.last_status)}</span></div><div class="meta"><span>Last checked: ${m.last_checked_at?new Date(m.last_checked_at).toLocaleString():'Never'}</span><button class="check">Check now</button></div>${c?`<div class="change"><b>Latest change · ${new Date(c.created_at).toLocaleDateString()}</b>${(c.added_lines||[]).slice(0,3).map(x=>`<div class="plus">+ ${esc(x)}</div>`).join('')}${(c.removed_lines||[]).slice(0,3).map(x=>`<div class="minus">− ${esc(x)}</div>`).join('')}</div>`:''}`;el.querySelector('.check').onclick=()=>check(m.id);list.appendChild(el)})}catch(e){msg.textContent=e.message}}
async function check(id){msg.textContent='Checking…';try{const d=await api(`/api/check?id=${encodeURIComponent(id)}`,{method:'POST'});msg.textContent=d.changed?'Pricing change detected.':'No pricing change detected.';await load()}catch(e){msg.textContent=e.message}}
$('#form').onsubmit=async e=>{e.preventDefault();msg.textContent='Capturing first snapshot…';try{const d=await api('/api/monitors',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:$('#name').value,url:$('#url').value})});$('#name').value='';$('#url').value='';msg.textContent=d.warning?`Added, initial check warning: ${d.warning}`:'Monitor added and first snapshot captured.';await load()}catch(e){msg.textContent=e.message}};$('#refresh').onclick=load;function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}load();
